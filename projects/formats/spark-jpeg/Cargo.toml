[package]
name = "spark-jpeg"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
readme.workspace = true
description = "Spark JPEG 解码 → TextureUpload（pure Rust `jpeg-decoder`）"
keywords = ["spark", "jpeg", "texture", "format"]
categories = ["game-development", "parsing"]
publish = false

[dependencies]
spark-core = { workspace = true }
spark-texture = { workspace = true }
jpeg-decoder = { workspace = true }
